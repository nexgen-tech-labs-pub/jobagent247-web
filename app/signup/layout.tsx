import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign up — JobAgent247',
  description: 'Create a JobAgent247 account. Founding-member trial: first 100 users get 30 days of Pro free.',
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
